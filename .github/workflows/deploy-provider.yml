# 部署 qtcrowd-provider：推送 tag（provider/*，如 provider/v0.1.0-alpha.1）时，
# 先构建发布 Docker 镜像到阿里云 ACR，再通过 Terraform apply 部署到阿里云 FC 3.0。
# 对齐 qtcloud-crowd 已跑通的部署模式。
#
# 所需 GitHub secrets（org 级，已有）：
#   - ALIYUN_ACCESS_KEY_ID / ALIYUN_ACCESS_KEY_SECRET：RAM 用户 AccessKey（terraform apply 用，
#     需 OSS/FC/RAM 管理权限；同时作为 provider 运行时访问 OSS 数据桶的 AK/SK 注入 FC 环境变量）
#   - ALIYUN_ACR_USERNAME / ALIYUN_ACR_PASSWORD / ALIYUN_ACR_REGISTRY：ACR 固定凭证
#     （docker login 用；REGISTRY 为本实例公网地址 crpi-xxx.personal.cr.aliyuncs.com）
#
# 前置条件：
#   - ACR 仓库 quanttide/qtcrowd-provider 已创建（PUBLIC，FC 免凭证直拉）
#   - OSS 状态桶 quanttide-terraform-state 已存在
#   - OSS 数据桶 qtcrowd-provider 已存在（手动创建——terraform 不建桶，fc.tf 只引用）
#   - FC 函数首次 apply 前需在阿里云开通 FC 3.0 并完成服务授权
#
# state key：qtcrowd/terraform.tfstate（provider 资源；site 资源由 deploy-site.yml 用
# qtcrowd/site.tfstate 管理——同一 terraform 目录两个 state，apply 均用 -target 限定各自资源）

name: Deploy Provider

on:
  push:
    tags:
      - 'provider/**'

concurrency:
  group: deploy-provider
  cancel-in-progress: false

env:
  TF_VAR_region: cn-hangzhou
  TF_VAR_environment: prod
  TF_STATE_BUCKET: quanttide-terraform-state
  TF_STATE_KEY: qtcrowd/terraform.tfstate
  # 后台 API 根 URL（qtcrowd-provider 的 QTCLOUD_CROWD_BACKEND_API，必填）：上架拉取 +
  # 认领/交付转发目标。qtcloud-crowd API 网关地址（公开，见 qtcloud-crowd README）
  TF_VAR_backend_api: https://api.quanttide.com/qtcloud-crowd
  # ACR 实例地址走 secret（ALIYUN_ACR_REGISTRY）：个人版实例专属域名属敏感信息，不写入代码
  ACR_NAMESPACE: quanttide

jobs:
  build-image:
    name: Build & Push Image
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # tag provider/v0.1.0-alpha.1 → 镜像 tag v0.1.0-alpha.1（仅 tag 触发，无手动入口）
      - name: Resolve Image Tag
        id: image
        run: |
          TAG="${GITHUB_REF_NAME#provider/}"
          echo "tag=${TAG}" >> "$GITHUB_OUTPUT"

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to ACR
        uses: docker/login-action@v3
        with:
          registry: ${{ secrets.ALIYUN_ACR_REGISTRY }}
          username: ${{ secrets.ALIYUN_ACR_USERNAME }}
          password: ${{ secrets.ALIYUN_ACR_PASSWORD }}

      - name: Build and Push
        uses: docker/build-push-action@v6
        with:
          context: src/provider
          push: true
          # 关键：关闭 provenance/sbom attestation——其 manifest 平台为 unknown/unknown，
          # FC 3.0 镜像优化器会拒绝（ImageOptimizingFailed: platform of image is unknown/unknown）
          provenance: false
          sbom: false
          tags: |
            ${{ secrets.ALIYUN_ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/qtcrowd-provider:${{ steps.image.outputs.tag }}
            ${{ secrets.ALIYUN_ACR_REGISTRY }}/${{ env.ACR_NAMESPACE }}/qtcrowd-provider:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Terraform Apply
    runs-on: ubuntu-latest
    needs: build-image
    timeout-minutes: 30
    permissions:
      contents: read
    # 生产环境保护：可在 GitHub Settings → Environments → production 配置 required reviewers（人工审批后才能 apply）
    environment: production

    env:
      # terraform / aliyun CLI 凭据
      ALICLOUD_ACCESS_KEY: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
      ALICLOUD_SECRET_KEY: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}
      ALICLOUD_ACCESS_KEY_ID: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
      ALICLOUD_ACCESS_KEY_SECRET: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}
      ALICLOUD_REGION_ID: cn-hangzhou
      # provider 运行时访问 OSS 数据桶（qtcrowd-provider）的 AK/SK（注入 FC 环境变量，见 manifests/terraform/fc.tf）
      TF_VAR_oss_access_key_id: ${{ secrets.ALIYUN_ACCESS_KEY_ID }}
      TF_VAR_oss_access_key_secret: ${{ secrets.ALIYUN_ACCESS_KEY_SECRET }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 部署镜像固定指向 ACR（FC 中国区拉不到 Docker Hub）；仅写入 GITHUB_ENV，不进 job output
      - name: Resolve Image Name
        run: |
          TAG="${GITHUB_REF_NAME#provider/}"
          echo "TF_VAR_image=${{ secrets.ALIYUN_ACR_REGISTRY }}/${ACR_NAMESPACE}/qtcrowd-provider:${TAG}" >> "$GITHUB_ENV"

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.15.8' # 对齐本地环境

      - name: Terraform Init（OSS 远程状态）
        working-directory: manifests/terraform
        run: |
          terraform init -input=false \
            -backend-config="bucket=${{ env.TF_STATE_BUCKET }}" \
            -backend-config="key=${{ env.TF_STATE_KEY }}" \
            -backend-config="region=${{ env.TF_VAR_region }}"

      - name: Terraform Validate
        working-directory: manifests/terraform
        run: terraform validate -no-color

      # -target 限定 provider 资源（site 资源由 deploy-site.yml 的 qtcrowd/site.tfstate 管理，
      # 不带 -target 会把 site 资源纳入本 state 导致冲突）
      - name: Terraform Plan
        working-directory: manifests/terraform
        run: terraform plan -input=false -no-color -out=tfplan \
          -target=alicloud_ram_role.fc \
          -target=alicloud_ram_role_policy_attachment.fc_vpc \
          -target=alicloud_fcv3_function.this \
          -target=alicloud_fcv3_trigger.http

      - name: Terraform Apply
        working-directory: manifests/terraform
        run: terraform apply -input=false -no-color tfplan
